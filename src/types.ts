export interface Experience {
  title: string;
  companyName: string;
  iconName: string;
  iconBg: string;
  date: string;
  points: string[];
}

export interface ContactForm {
  name: string;
  email: string;
  message: string;
}
